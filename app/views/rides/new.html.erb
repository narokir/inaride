<% provide(:title, 'Create New Ride')%>

<div class="row-fluid">
  <div class="proped">
    <div class='embos'>
      <div class="span8 ">
	<%= simple_form_for(@ride,:html => {:class => 'form-horizontal' }) do |f| %>
	<%= render 'fields', f: f %>
      <% end %>
    </div>
    <div class="span4">
      <%= render 'map' %>
    </div>
    </div>  
  </div>  
</div>


