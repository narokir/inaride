<% provide(:title, 'Create New Ride')%>

<div class="row-fluid">
  
  <div class="span8 embos">
      <h1>New Ride</h1><hr>
      <%= simple_form_for(@ride,:html => {:class => 'form-horizontal' }) do |f| %>
      <%= render 'fields', f: f %>
      <div class="controls">
	<% if signed_in? %>
	  <%= f.submit "Done", class: "btn btn-large btn-success" %>
	<% else %>
	  <%= link_to('Please sign in to post this ride', "#signinModal", :id => 'login', "data-toggle" => "modal") %>
	  <%= f.submit "Done", html = { id: "submit_new_ride", class: "btn btn-large btn-success",:disabled => true }  %>
	<% end %>
      </div>
    <% end %>
  </div>
  <div class="span4 embos">
    <%= render 'map' %>
  </div>
  
</div>
  
<!--<a class="tooltip-left"href="#" title="A nice tooltip" >Tooltip Left</a>
<a class="tooltip-right"href="#" title="A nice tooltip" >Tooltip Right</a>
<a class="has-popover"href="#">Popover</a>-->


