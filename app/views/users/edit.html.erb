<% provide(:title, 'Edit Proflie') %>
<div id="main" class="container">
  <div class="row-fluid">
    <div class="embos">
	<h1>Edit Profile</h1><hr>
	<%= simple_form_for(@user,:html => {:class => 'form-horizontal' }) do |f| %>
	<%= render 'fields', f: f %>
  
	<div class="controls">
	<%= f.submit "Save Changes", class: "btn btn-large btn-success" %>
	</div>
      <% end %>
    </div>
  </div>
</div>