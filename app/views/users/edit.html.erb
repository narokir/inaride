<div class="row-fluid">
  <div class="embos span6">
      <h1>Edit ride</h1><hr>
      <%= simple_form_for(@ride,:html => {:class => 'form-horizontal' }) do |f| %>
      <%= render 'fields', f: f %>
      <div class="controls">
      <%= f.submit "Save Changes", class: "btn btn-large btn-success" %>
      </div>
    <% end %>
  </div>
</div>